name: ClipShelf
options:
  bundleIdPrefix: com.nicebro
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"

settings:
  base:
    DEVELOPMENT_TEAM: ${CLIPSHELF_DEVELOPMENT_TEAM}

configFiles:
  Debug: Config/Signing.xcconfig
  Release: Config/Signing.xcconfig

targets:
  ClipShelf:
    type: application
    platform: macOS
    sources:
      - Sources
    dependencies:
      - sdk: JavaScriptCore.framework
      - sdk: libsqlite3.tbd
      - sdk: AppIntents.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nicebro.ClipShelf
        PRODUCT_NAME: ClipShelf
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_FILE: Sources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Sources/ClipShelf.entitlements
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        CODE_SIGN_STYLE: Automatic
        ENABLE_HARDENED_RUNTIME: YES
    entitlements:
      path: Sources/ClipShelf.entitlements

  ClipShelfTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: ClipShelf
    settings:
      base:
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        GENERATE_INFOPLIST_FILE: YES
