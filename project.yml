name: ClipShelf
options:
  bundleIdPrefix: com.nicebro
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15.0"

targets:
  ClipShelf:
    type: application
    platform: macOS
    sources:
      - Sources
    dependencies:
      - sdk: JavaScriptCore.framework
      - sdk: libsqlite3.tbd
      - sdk: AppIntents.framework
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.nicebro.ClipShelf
        PRODUCT_NAME: ClipShelf
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        INFOPLIST_FILE: Sources/Info.plist
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: "-"
        ENABLE_HARDENED_RUNTIME: YES
    entitlements:
      path: Sources/ClipShelf.entitlements
      properties:
        com.apple.security.app-sandbox: false

  ClipShelfTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - Tests
    dependencies:
      - target: ClipShelf
    settings:
      base:
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        GENERATE_INFOPLIST_FILE: YES
